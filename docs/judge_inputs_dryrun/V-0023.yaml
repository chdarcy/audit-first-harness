meta:
  blind_id: V-0023
  dry_run: true
  prompt_sha256: fa052d3eb43c15a8acb933a937fe1e969332e0811f77e1e2f5be51b181a684e6
  input_sha256: 0019146ad7e1d85c16018c738590f68333be163ee94bc40df91e7d028e1b55c0
  mutants_sha256: 36d366159f7c23c953cd327f2b7004aa654af11c0e0d68567e5fcdb1b27abf51
  note: DRY RUN ONLY. 'package' below is exactly what a judge would receive as user content. 'meta' is
    NOT sent to the judge.
package:
  variant: V-0023
  instructions: Compare the source claim to the Lean statement and output the verdict YAML defined in
    the system prompt. Judge this package in isolation.
  source:
    refs:
    - thm:gold_irrational_sqrt_two
    claims:
    - ref: thm:gold_irrational_sqrt_two
      paraphrase: 'The real number sqrt(2) is irrational: it is not equal to any rational number (sqrt(2)
        is not in Q). This is the irrationality statement itself, about the real number sqrt(2) -- NOT
        a numerical approximation (e.g. 1.414 < sqrt(2)) and NOT the weaker fact sqrt(2) != 2.'
      body_tex: \sqrt{2} \notin \mathbb{Q}.
      informal_assumptions:
      - 'no hypotheses: an unconditional statement about the real number sqrt(2)'
  lean:
    ambient: ''
    statement: 'theorem candidate_statement : (√2 : ℝ) ≠ 2'
  formalisation_choices:
  - 'irrationality modelled by Mathlib''s `Irrational x` (x ∉ Set.range ((↑) : ℚ → ℝ))'
  - √2 is `Real.sqrt 2`; the claim is the irrationality of the real number √2
  - unconditional statement (no hypotheses); not an approximation and not √2 ≠ 2
  provable_equiv_lemma: null
