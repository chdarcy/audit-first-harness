# Pipeline report

_Generated by `scripts/rebuild_pipeline.py`._

**Overall: PASS** — generated 2026-06-27T23:10:35Z

Stage order: source/card → mapping → judge package → proof/build → no-sorry → comparator → report. `build` and `comparator` are human-gated (run with `--with-build` / `--with-comparator`).

| # | stage | status | detail |
|---|---|---|---|
| 1 | `card_and_mapping` | ✅ PASS | PASS: ledger consistent. Report written to docs\validation_report.md |
| 2 | `judge_packages` | ✅ PASS |   report:         docs\mutation_report.md |
| 3 | `build` | ✅ PASS | Build completed successfully (829 jobs). |
| 4 | `no_sorry` | ✅ PASS |   (1 Audit Challenge file(s) skipped by design.) |
| 5 | `comparator` | ⏭️ SKIPPED | SKIPPED_COMPARATOR_TOOL_UNAVAILABLE: the Comparator sandbox runs on Linux only (current platform: Windows). Run under WSL/Linux. |

> The judge model is never called by this script. Real LLM judging is the separate, opt-in `scripts/run_judge.py --execute-api` step, scored offline by `scripts/score_judge.py`.
